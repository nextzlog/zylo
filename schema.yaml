type: object
patternProperties:
  (cfg|dat|dll|exe):
    type: object
    patternProperties:
      '[0-9A-Za-z]+':
        type: object
        properties:
          tag:
            type: string
          sum:
            type: string
          msg:
            type: string
          url:
            type: string
          web:
            type: string
          use:
            type: array
            items:
              type: string
        required:
          - tag
          - msg
          - url
          - web
    additionalProperties: false
additionalProperties: false
